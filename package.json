{
	"name": "typescript-typeorm",
	"version": "1.0.0",
	"description": "",
	"main": "src/index.ts",
	"scripts": {
		"dev": "concurrently -n \"NODEMON,BUILDER\" -c \"yellow,blue\" -p \"[{name}]\" \"npm run dev-nodemon\" \"npm run dev-build\"",
		"dev-nodemon": "nodemon dist/index.js",
		"dev-build": "tsc --watch",
		"build-pre": "rimraf dist",
		"build": "npm run build-pre && tsc --build"
	},
	"author": "Alejo Kim Uy",
	"license": "ISC",
	"devDependencies": {
		"nodemon": "^3.0.2"
	},
	"dependencies": {
		"@types/node": "^20.10.8",
		"mysql2": "^3.7.0",
		"reflect-metadata": "^0.2.1",
		"rimraf": "^5.0.5",
		"typeorm": "^0.3.19"
	}
}
